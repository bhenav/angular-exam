import { DashboardNavToggleDirective } from './dashboard-nav-toggle.directive';

describe('DashboardNavToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new DashboardNavToggleDirective();
    expect(directive).toBeTruthy();
  });
});
